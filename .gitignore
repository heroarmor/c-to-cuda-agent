# Compiled C / CUDA artifacts
*.o
*.obj
*.out
*.exe
*.so
*.a
a.out

# CUDA / build outputs
*.ptx
*.cubin
*.fatbin
build/
bin/

# Python (agent tooling)
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.ipynb_checkpoints/

# Environment / secrets
.env
.env.local

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store

# Personal notes (not tracked)
explain.md

# opencode: track config + custom commands, ignore local deps/state
.opencode/node_modules/
.opencode/package.json
.opencode/package-lock.json
.opencode/bun.lock
.opencode/.gitignore

# image outputs written to CWD by mandelbrot/raytrace/pathtrace runs
# (the reference images under benchmark/ stay tracked)
/*.pgm
/*.ppm
