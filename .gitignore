# Compiled C / CUDA artifacts
*.o
*.obj
*.out
*.exe
*.so
*.a
a.out

# CUDA / build outputs
*.ptx
*.cubin
*.fatbin
build/
bin/

# macOS
.DS_Store

# Claude Code local settings (personal, not shared)
.claude/settings.local.json

# Python
__pycache__/
*.pyc
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.ipynb_checkpoints/

# Editor/IDE
.vscode/
.idea/
*.swp

# Personal notes (not tracked)
explain.md

# llama2 checkpoint/tokenizer data generated by benchmark/complex/dnn/llama2_gen_checkpoint
*.bin

# image outputs written to CWD by mandelbrot/raytrace/pathtrace runs
# (the reference images under benchmark/ stay tracked)
/*.pgm
/*.ppm

# generated evaluation reports / raw TSVs (framework + .gitkeep stay tracked)
evaluation/results/*
!evaluation/results/.gitkeep

# agent run outputs (reproducible by re-running the scripts)
generated/
agent_pipeline/runs/
test_results/
dataset_eval/reports/

# polyhedral toolchain build tree (rebuild with polyhedral/build_ppcg.sh)
polyhedral/toolchain/
polyhedral/toolchain-build.log
polyhedral/verify-gpu.log
polyhedral/moves-smoke.log

# opencode dev checkout the pipeline drives via `bun dev run` (clone of
# github.com/sst/opencode next to the repo -- see agent_pipeline/ARCHITECTURE.md)
/opencode/
polyhedral/phase3-e2e.log
