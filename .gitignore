# macOS
.DS_Store

# Claude Code local settings (personal, not shared)
.claude/settings.local.json

# dataset_zyj build output: compiled benchmark binaries and the synthetic
# llama2 checkpoint/tokenizer data, both regenerated by `make all`
dataset_zyj/bin/
dataset_zyj/data/

# Python
__pycache__/
*.pyc

# Editor/IDE
.vscode/
.idea/

# Unrelated nested project (Univer spreadsheet checkout, not part of this
# project's history)

# agent_baseline/run_baseline.py output: generated CUDA translations,
# opencode transcripts, and run results. Reproducible by re-running the
# script, not hand-written.
generated/

# agent_pipeline/run_pipeline.py output: per-run stage logs and
# pipeline_result.json summaries. Reproducible by re-running the script.
agent_pipeline/runs/

test_results/

# dataset_eval/run_dataset.py output: per-run benchmark summary tables.
# Reproducible by re-running the script.
dataset_eval/reports/